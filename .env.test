# Environment type (test, development, production)
NODE_ENV=test

# Server
PORT=8000
HOST=127.0.0.1
BASE_URL=http://localhost:8000

# Database (mock)
MONGO_URI=mongodb://localhost:27017/testdb

# Model (mock path)
MODEL_PATH=/tmp/mock-model.gguf
model=/tmp/mock-model

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Hugging Face (unused in tests)
HF_REPO_ID=
HF_FILENAME=
HF_LOCAL_DIR=

# Firebase (stub)
FIREBASE_CREDENTIAL_JSON={"type":"service_account","project_id":"demo"}

# Test mode flag
TEST_MODE=true
